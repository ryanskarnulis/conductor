// Placeholder page for Slice 1 (scaffold only). Later slices replace this
// with the delegate chat UI, mirroring PCC's features/agent/AgentPage.tsx.
function App() {
  return (
    <main className="conductor-shell">
      <h1>Conductor</h1>
      <p className="conductor-tagline">
        The fleet's master agent. Routes requests to the right app agent —
        coming online in a later slice.
      </p>
    </main>
  )
}

export default App
